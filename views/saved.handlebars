{{!-- <h1>Android Police News Scraper</h1> --}}

{{!-- <button class="btn btn-primary"><a href="/scrape">Scrape Now</a></button> --}}

<div class="jumbotron">
    <h1 class="display-4">Android Police News Scraper</h1>
    <p class="lead">This website grabs the most recent articles from the popular tech website Android Police and gives
        you the title, a brief description, and a link to the article.</p>
    {{!-- <hr class="my-4"> --}}
    <br>
    <p>Just click 'Scrape now' to get started. You can also save articles for later viewing by clicking 'Save'. You can
        find all your saved articles in the 'Saved articles' tab.</p>
    <a class="btn btn-dark btn-lg" href="/scrape" role="button">Scrape now</a>
    <a class="btn btn-warning btn-lg" href="/clear" role="button">Clear all</a>
</div>

<div class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="https://androidpolice.com" target="blank"><img class="ap-logo"
            src="/images/ap-logo-light.png" </a> <button class="navbar-toggler" type="button" data-toggle="collapse"
            data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">
                        <h3 class="font-weight-light">Home </h3>
                    </a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/saved">
                        <h3 class="font-weight-light">Saved articles</h3><span class="sr-only">(current)</span>
                    </a>
                </li>
            </ul>
        </div>
</div>

<div class="container">
    {{#if articles}}
    {{#each articles}}

    {{!-- <div class="card">
        <div class="card-body">
            <h5 class=" card-header card-title"><a href={{this.link}} target="blank">{{this.title}}</a></h5>
            <span><small>{{this.author}}</small></span><span> - </span><span><small>{{this.timeStamp}}</small></span>
            <hr>
            <p class="card-text">{{this.snippet}}</p>
            <button class="btn btn-primary">Save Article</button>
        </div>
    </div> --}}

    <div class="card">
        <h5 class=" card-header"><a class="title-link" id="title{{@index}}" href={{this.link}}
                target="blank">{{this.title}}</a></h5>
        <div class="card-body">
            <blockquote class="blockquote mb-0">
                <p class="card-text" id="snippet{{@index}}">{{this.snippet}}</p>
                <footer class="blockquote-footer">{{this.author}} <span><small>- </small></span><cite
                        title="Source Title">{{this.timeStamp}} </cite>
                </footer>
            </blockquote>
            <br>
            <a class="btn btn-outline-danger btn-lg btn-block delete-button" id="{{@index}}" role="button">Delete</a>
        </div>
    </div>
    <br>
    {{/each}}
    {{else}}
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">I'm not seeing any articles</h4>
            <h6>Try saving one from the home page</h6>
        </div>
    </div>
    {{/if}}
</div>